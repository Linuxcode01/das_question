package LeetCode.Recursion;

import java.util.Arrays;

public class reverseArray {
    public static void revArray(int []arr){
//        Arrays.sort(arr);
        if(arr.length == 1){
            return;
        }
        int lstEel = arr[arr.length - 1];

        for (int i = 0; i < arr.length; i++){
            if(arr[lstEel] < arr[i]){

            }
        }

    }
    public static void main(String[] args) {

    }
}
